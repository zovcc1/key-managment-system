# 13. الفجوات والمهام اليدوية المطلوبة من الطالب (Gaps)

هذا الملف يجمّع كل ما **لا يمكن استخراجه من الكود المصدري** لهذا المستودع، مهما بلغ عمق الفحص. كل بند هنا موثّق كحقيقة سلبية مؤكَّدة (تم البحث فعلياً ولم يوجد شيء)، وليس تخميناً بالغياب. لكل بند: ماذا يعني الغياب، لماذا لا يمكن سدّه من داخل المستودع، وما الذي يجب على الطالب فعله يدوياً لإكمال الأطروحة.

## 13.1 تاريخ Git والتسلسل الزمني للتطوير

**الحالة**: `[غير موجود في الكود]`

لا يوجد مستودع Git في `/home/kasem/Documents/freelance/jehad`. تنفيذ `git status` يعيد `fatal: not a git repository (or any of the parent directories): .git`، وهو ما جرى التحقق منه مباشرة في هذه الجلسة (وليس نقلاً عن الخطة فقط). نتيجة لذلك، كل ما يلي غير موجود في الكود ولا يمكن استخراجه من أي ملف في المستودع:

- تاريخ إنشاء المشروع الفعلي وتاريخ آخر تعديل حقيقي (طابع الوقت الوحيد المتاح في كامل المستودع هو `Create Date: 2026-08-23 23:35:58.689721` داخل تعليق ترحيل Alembic الوحيد `alembic/versions/e1a463aef094_initial_schema.py`، وهو تاريخ توليد الملف آلياً وليس تاريخ بدء المشروع).
- عدد المساهمين (contributors) وأسماؤهم.
- رسائل الـ commits وتسلسل تطوّر الميزات عبر الزمن.
- الفروع (branches) التاريخية وعمليات الدمج (merges).
- أي مقياس على نمط "سرعة التطوير" أو "عدد التعديلات لكل ملف" أو "أكثر الملفات تعديلاً".

**المطلوب من الطالب**: إذا كانت الأطروحة تتطلب فصل "منهجية التطوير" أو "الجدول الزمني للمشروع"، يجب أن يُبنى هذا الفصل من مصدر خارج الكود تماماً — مثل سجلات شخصية، أو مذكرات تطوير، أو الرجوع لمصمّم/مطوّر المشروع الأصلي لسؤاله مباشرة عن التسلسل الزمني الفعلي. لا يمكن لأي أداة تحليل استاتيكي أن تنتج هذه المعلومة من الكود وحده. ترتيب طبقات البناء المستنتج في `10_METHODOLOGY.md` (عبر تحليل شجرة الاستيراد import graph) هو **استنتاج منطقي لترتيب الاعتماد البنيوي بين الوحدات**، وليس تسلسلاً زمنياً فعلياً لكتابة الكود — يجب عدم الخلط بين الاثنين عند الكتابة.

## 13.2 مرجع المواصفات الخارجي ("section 3" / "Deliverable, section 8")

**الحالة**: `[غير موجود في الكود]`

الكود يحتوي على 9 إشارات صريحة إلى وثيقة مواصفات خارجية غير موجودة في المستودع:

| الموقع | نص الإشارة |
|---|---|
| `keyring/seed.py:1` | `"""Seed command (Deliverable, section 8).` |
| `keyring/core/certificate.py:1` | `"""Signed erasure certificates (section 3). Signed with HMAC-SHA256 under a` |
| `keyring/tests/test_destroy.py:2` | `key exemption (section 3)."""` |
| `keyring/models/keys.py:74` | `# Destruction tombstone (crypto-shredding proof, section 3). The wrapped` |
| `keyring/tests/test_crypto_shredding.py:1` | `"""Crypto-shredding / subject erasure tests (section 3)."""` |
| `keyring/core/service.py:189` | `# (section 3), not a hazard to guard against.` |
| `keyring/core/service.py:198` | `# Metadata-only tombstone (section 3): the wrapped key row is` |
| `keyring/core/service.py:549` | `# Crypto-shredding / erasure (section 3)` |
| `keyring/models/enums.py:52` | `# active -> destroyed transition *is* crypto-shredding (section 3): a` |

جرى البحث عن أي ملف يحمل اسم "Deliverable" أو "spec" أو "requirements" أو "SRS" في كامل المستودع (`find . -iname '*deliverable*' -o -iname '*spec*'`) دون العثور على أي وثيقة مطابقة. هذه الإشارات تفترض وجود مستند مواصفات بناء (build specification / SRS) خارجي كان مرجعاً أثناء كتابة الكود، لكنه لم يُدرَج في هذا المستودع.

**المطلوب من الطالب**: الحصول على وثيقة "Deliverable" الأصلية (على الأرجح من الجهة المُصدرة للمواصفات أو من مطوّر المشروع)، وتحديداً "section 3" (يبدو أنها القسم الخاص بآلية crypto-shredding) و"section 8" (القسم الخاص بأمر seed/بيانات العرض التوضيحي). إن تعذّر الحصول عليها، يجب أن تُكتب الأطروحة بالاعتماد الكامل على السلوك الفعلي المُلاحَظ في الكود (الذي جرى توثيقه بالكامل في `04_DATABASE.md`, `06_SECURITY.md`, `10_METHODOLOGY.md`) دون افتراض أي تفاصيل إضافية من الوثيقة غير الموجودة.

## 13.3 نتائج تجريبية وقياسات أداء (Performance/Benchmarks)

**الحالة**: `[غير موجود في الكود]`

لا يوجد في المستودع أي:
- سكربت قياس أداء (benchmark) أو ملف نتائج قياس.
- قياسات زمن استجابة API فعلية تحت حمل (load testing).
- مقارنة بين مزوّدي المفاتيح الأربعة (file/env/vault/kms) من ناحية الأداء.
- أي تقرير `pytest --durations` أو ما شابه محفوظاً في المستودع.

جرى التحقق من عدم وجود أي ملف بامتداد `.json`/`.csv`/`.md` يحمل نتائج قياس زمني أو استهلاك موارد.

**المطلوب من الطالب**: إن كانت الأطروحة تتطلب فصل "التقييم والنتائج التجريبية" (evaluation)، يجب على الطالب تشغيل المشروع فعلياً (باتباع خطوات التشغيل الموثّقة حرفياً في `10_METHODOLOGY.md`) وإجراء القياسات بنفسه — مثل زمن تنفيذ عملية `encrypt`/`decrypt` واحدة، زمن دورة `rewrap` الكاملة لعدد معيّن من الأظرف (envelopes)، أو مقارنة أداء SQLite مقابل PostgreSQL (المشروع مُصمَّم لـ PostgreSQL في الإنتاج حسب `README.md` لكنه يعمل بـ SQLite افتراضياً في وضع التطوير).

## 13.4 المشرف الأكاديمي وبيانات الأطروحة الرسمية

**الحالة**: `[غير موجود في الكود]`

لا يوجد في المستودع أي ملف `LICENSE`, `AUTHORS`, `CONTRIBUTING`, أو `CHANGELOG`، ولا حقل `authors` أو `maintainers` في `pyproject.toml` (تم التحقق مباشرة عبر `grep` بلا نتائج). لا اسم مشرف أكاديمي، لا اسم جامعة أو كلية، لا رقم تسجيل طالب، ولا أي بيانات إدارية متعلقة بالأطروحة نفسها موجودة في أي مكان في الكود.

**المطلوب من الطالب**: هذه بيانات إدارية بحتة يجب إضافتها يدوياً من خارج المستودع (اسم المشرف، الجامعة، القسم، الفصل الدراسي، إلخ) حسب متطلبات الجامعة الرسمية لصفحة الغلاف وصفحة الإقرار.

## 13.5 إصدارات البنية التحتية غير المثبَّتة (PostgreSQL / Node.js)

**الحالة**: `[غير موجود في الكود]`

- **PostgreSQL**: `README.md` و`pyproject.toml` يذكران أن المشروع "مصمَّم لـ PostgreSQL" (عبر تبعية `psycopg[binary]>=3.2`)، لكن لا يوجد أي تثبيت لإصدار PostgreSQL محدَّد (لا `docker-compose.yml`, لا ملف تهيئة يحدد رقم إصدار). قاعدة البيانات الفعلية المستخدَمة والمبذورة في هذا المستودع (`keyring.db`) هي SQLite، لا PostgreSQL.
- **Node.js**: لا يوجد ملف `.nvmrc` ولا `.node-version` ولا حقل `engines` في `web/package.json` يحدد إصدار Node.js المطلوب لتشغيل الواجهة الأمامية. تم التحقق من غياب هذه الملفات مباشرة.

**المطلوب من الطالب**: إذا كانت الأطروحة تتطلب ذكر "متطلبات النظام" (system requirements) بدقة، يجب تحديد إصدار PostgreSQL وNode.js المُستخدَمين فعلياً وقت كتابة الأطروحة (مثلاً عبر `postgres --version` و`node --version` على بيئة التطوير الفعلية)، لأن الكود لا يفرض أي قيد إصدار صريح على أي منهما.

## 13.6 التكامل الفعلي مع HashiCorp Vault وخدمة KMS

**الحالة**: مُختبَر بمحاكاة (mock) فقط، لا يوجد تكامل فعلي أو نشر تجريبي موثَّق.

مزوِّدا المفاتيح `VaultKeyProvider` (`keyring/providers/vault_provider.py`، 84 سطراً) و`KMSKeyProvider` (`keyring/providers/kms_provider.py`، 82 سطراً) ينفّذان استدعاءات HTTP فعلية عبر `httpx.Client` — `VaultKeyProvider` يستهدف نقاط نهاية HashiCorp Vault Transit engine الحقيقية (`/v1/sys/health`, `/v1/{mount}/keys/{ref}`, `/v1/{mount}/encrypt/{ref}`, `/v1/{mount}/decrypt/{ref}`، كما في `vault_provider.py:34,64,70,77,83-84`)، وهذا تنفيذ حقيقي وليس كوداً وهمياً (stub). لكن الاختبارات الوحيدة الموجودة لهما في `keyring/tests/test_providers.py` تستخدم كائن `_FakeHttpxModule` مُحاكٍ (`test_providers.py:41-134`) بدلاً من اتصال فعلي بخادم Vault أو خدمة KMS حقيقية — أي أنه **لا يوجد أي اختبار تكامل (integration test) فعلي ضد نسخة حقيقية من Vault أو أي خدمة KMS** في هذا المستودع، ولا يوجد أي `docker-compose.yml` لتشغيل Vault محلياً للتجربة.

**المطلوب من الطالب**: إن رغب الطالب في تغطية هذين المزوِّدين عملياً في الأطروحة (مثلاً في فصل "التحقق والاختبار")، عليه تشغيل خادم Vault فعلي (عبر `vault server -dev` مثلاً) وضبط متغيرات البيئة `settings.vault_addr` و`settings.vault_token_env_var` (الأسماء فقط — القيم الفعلية لا تُنسخ لأي وثيقة) يدوياً، ثم إجراء اختبار تكامل حي بنفسه. الكود لا يوفر هذا التحقق جاهزاً.

## 13.7 عدد الجداول: توضيح رقمي نهائي (لا تعارض متبقٍ)

كانت الخطة الأصلية قد أشارت إلى تعارض محتمل بين عدّ "13" وعدّ "14" جدولاً. جرى حسم هذا التعارض بالكامل ضمن `04_DATABASE.md` وأُعيد التحقق منه مرة أخرى في `12_METRICS.md` §12.5 عبر استعلام مباشر على `keyring.db`: العدد الصحيح والنهائي هو **14 جدولاً موضوعياً** (`alerts, approvals, audit_log, decrypt_failures, erasure_certificates, idempotency_records, keks, operators, system_settings, rewrap_jobs, sessions, subject_keys, envelopes, rewrap_failures`)، زائد جدول `alembic_version` الداخلي (15 إجمالاً). لا حاجة لأي عمل يدوي إضافي هنا — هذا البند مُدرَج للتوثيق فقط، لتأكيد أن التعارض المذكور في مرحلة التخطيط قد حُسم فعلياً بالفحص المباشر ولم يعد فجوة مفتوحة.

## 13.8 ملخّص الفجوات (جدول سريع للطالب)

| # | الفجوة | نوعها | إجراء الطالب المطلوب |
|---|---|---|---|
| 1 | تاريخ Git وتسلسل التطوير الزمني | غياب كامل من الكود | جمع من مصادر خارجية (سجلات شخصية/سؤال المطوّر الأصلي) |
| 2 | وثيقة المواصفات الخارجية ("section 3" / "Deliverable, section 8") | مرجع غير موجود | الحصول على النسخة الأصلية أو الاعتماد الكامل على السلوك المُلاحَظ في الكود |
| 3 | نتائج تجريبية وقياسات أداء | غير موجودة، تتطلب تنفيذاً فعلياً | تشغيل المشروع وإجراء القياسات يدوياً |
| 4 | بيانات المشرف الأكاديمي وغلاف الأطروحة | إدارية، خارج نطاق الكود بالكامل | تعبئة من الجامعة مباشرة |
| 5 | إصدار PostgreSQL وNode.js الدقيقين | غير مثبَّت في أي ملف تهيئة | تحديد الإصدار الفعلي المُستخدَم في بيئة الطالب |
| 6 | تكامل فعلي مع Vault/KMS | مُختبَر بمحاكاة فقط | تشغيل خادم Vault حقيقي إن رغب الطالب بتغطية عملية |
| 7 | عدد الجداول (13 مقابل 14) | **محسوم بالفعل** | لا إجراء مطلوب — للتوثيق فقط |

هذا الملف هو الأخير من أصل 13 ملفاً في `thesis-extraction/`. كل الحقائق التقنية القابلة للاستخراج من الكود موثّقة في الملفات 00–12؛ ما تبقّى هنا هو حصراً ما يستحيل استخراجه من المستودع نفسه.
